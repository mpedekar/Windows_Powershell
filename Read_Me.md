# Created just for testing. Although if I need to use it really, would say this created to explain for what purpose this repository stands for. This repository is primarily storing scripts which I need for day to day job to perform windows server administration.


New line added after I merged the files from github to local.
 again added new line.

 git init
 git status
 git log
 git branch
 git checkout <branch>
 git checkout <hash code of commit>
 git checkout -b <your name/what feature or code>  
 git add . (add all files to staging state)
 git add <file name>  (add particular file to staging state)
 git commit -m "message about what new things in the working directory"
 git merge <full branch path with branch name> 